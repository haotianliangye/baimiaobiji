# 白描笔记 — 项目级 Agent 规则

## 重命名安全检查

在对任何变量、函数、类型进行重命名后，必须执行以下步骤，否则不得提交：

1. 使用 `grep_search` 在整个相关目录中搜索**旧名称**，确认结果为零
2. 如果搜索到残留引用，逐一修复
3. 在 commit message 或改动说明中注明「已通过 grep 确认旧名称零残留」

此规则适用于所有重构场景，包括但不限于：
- 状态变量重命名（如 React useState）
- 函数/方法重命名
- 接口/类型重命名
- CSS 类名重命名

## 命名约定异常：保留产品标识符

尽管全局规则要求“不使用拼音”，但对于本项目的核心产品名称 **“白描” (baimiao)** 属于绝对例外：
- **禁止去拼音化**：在任何变量名、Storage 键名、API 路由或本地路径中（如 `baimiao_` 键前缀、`/baimiaobiji/` 目录），绝对禁止将 `baimiao` 翻译或重命名为 `whitewash` 或其他英文单词。
- `baimiao` 必须作为一个合法的专用名词在整个代码库中予以保留。

## 多模板生成存在性检查规约

由于白描笔记支持用户配置并同时启用多个不同的日记/回顾 Prompt 模板，在任何涉及日记（`daily_diaries`）和回顾（`daily_reviews`）的“已存在校验”、“生成检测”或“自动补全”逻辑中，必须遵守以下要求：

1. **禁止仅通过日期进行“有无”判断**：
   绝对禁止在查询到该日期已有一篇日记/回顾（例如使用 `.first()` 判空）后就直接忽略或退出。这会导致用户启用多 Prompt 模板时漏掉其它模板内容的生成。
2. **必须根据 Prompt 索引精确匹配**：
   必须提取当前所有启用的 Prompt 索引列表（如通过 `getActivePromptIndices`），与数据库中该日期下已存储数据记录的 `prompt_index` / `review_prompt_index` 集合进行差集对比，精确盘点具体是哪一个模板的数据，进而发起增量补全。

## 经典修长仿宋体垂直对齐规约

当项目需要清秀、修长且完全开源商用免版权的中文字体时，应当优先选用经典的**仿宋体（FangSong）**家族：

- **CSS 字体声明**：`font-family: FangSong, STFangsong, "华文仿宋", serif;`
- **视觉重心下移补偿**：由于仿宋体字身修长、笔画纤细，其视觉几何重心相对于普通的加粗黑体字天然略微偏上。在包含搜索、设置等动作图标的 Flex 垂直居中容器（`flex items-center`）中，**必须显式添加 `translate-y-[2px]` 进行物理下移补偿**，才能使 Logo 等文本与右侧按钮图标在视网膜上达成像素级的绝对垂直平齐。

## 文本卡片防抖动与抗虚化动效规约

对于包含细密文本或小字号（如 <16px）正文的交互气泡与卡片组件（例如 `.baimiao-card-bubble`），在定义其悬浮（Hover）与激活（Active）过渡效果时，必须遵循以下防渲染缺陷原则：

- **避免 Hover 物理位移**：在 Hover 态下**禁止使用 `transform: translateY`** 等会造成元素在物理空间产生位移的转换属性。位移动画会强迫浏览器进行合成层（Composited Layer）切换，在动画结束的一瞬间会因重新栅格化与抗锯齿切换产生肉眼可见的“文字变胖、变细、闪烁或虚化抖动”。
- **纯粹的光学漫射反馈**：推荐仅通过 `box-shadow`（如暮光紫渐变投影）和 `border-color`（如边框变淡紫）的过渡来提供悬浮反馈：
  `transition: box-shadow 0.3s ease, border-color 0.3s ease !important;`
- **按压反馈降级**：在移动端（`hover:none`）的按压反馈上，同样避免对包含密集文本的卡片使用 `scale` 缩放，推荐通过微调背景色与边框色深浅（如变更为 `bg-[#f6f4f9]`）来提供温润稳定的触感回馈。

## 中文排版两端对齐与短行/首段标题防拉伸规约

在使用全局两端对齐（`text-align: justify; text-justify: inter-ideograph;`）以提升中文排版高级感时，为了防止无 `#` 标题前缀的单行文本（如日记标题段落）被浏览器误判为段落首行而强行扯大字距（如 `道   痕   .   捞   石   头`），必须遵循以下安全对齐规约：

1. **强制最后一行靠左**：在定义两端对齐的 CSS 代码中，必须显式附加 `text-align-last: left !important;`。这能确保任何段落的最后一行以及单行段落绝对不被浏览器两端对齐拉伸。
2. **标题首段左对齐保护**：在日记等正文 Markdown 容器中，必须对首个段落（如 `.baimiao-editorial-body p:first-of-type`）显式覆盖为 `text-align: left !important;`，以防止模型未输出标题符号时第一行被误拉伸。

## 移动端虚拟键盘 Enter 键换行防拦截规约

在 `textarea` 等文本录入区域中设计“快捷回车发送（Enter 提交）”逻辑时，必须进行双端区分：

1. **PC 端**：允许拦截回车（不按 Shift 时）进行一键秒发，以保障键盘录入的高效性。
2. **移动端/手机端**：**绝对禁止**将回车键（虚拟键盘右下角的“换行/发送”）拦截为提交动作，这会严重打断用户的输入流。必须通过设备环境侦测（检测 `ontouchstart`、`maxTouchPoints` 或视口宽度）判定 `isMobile`，如果是移动端，则必须允许回车键执行天然换行，只通过点击发送按钮或收起键盘来触发表单提交。

## 大模型非标输出超链接“占位符保护清洗算法”规约

在大模型生成的数据中包含原始日志索引 UUID（如 `#log_id_UUID` 或 `[UUID]` 等）时，前端在将数据提交给 ReactMarkdown 渲染前，必须对其进行预处理清洗以实现高健壮性。

为了防止正则在清理杂乱的裸露 UUID 时误伤、破坏已经写好的标准链接（导致如 `[文字](#log_id_UUID)(#log_id_UUID)` 等裸露括号渲染故障），必须在清洗函数中实施“占位符保护机制”：

1. **标准链接置换锁死**：首先将所有正规书写的标准链接（如 `[文字](#log_id_UUID)`）提取出来，暂存入隔离数组，原位置替换为特定占位符（如 `__PRESERVED_LINK_PLACEHOLDER_x__`）锁定保护。
2. **杂乱非标 UUID 扫射清洗**：在标准链接被锁死后，对其余所有非标模式（例如行内代码块包裹的 `` `#log_id_UUID` ``、中括号裸 UUID `[UUID]`、不带引导头的裸 UUID 串）进行强制正则表达式匹配清洗，统一格式化统合为 `[引用](#log_id_UUID)`（若括号内有自定义描述文本，则保留该文本作为引用超链接字符）。
3. **还原占位符**：最后将之前锁死保护的完美超链接从数组中按索引还原替换回来，从而实现对大模型生成数据的高抗噪和 100% 高保真气泡跳转标签渲染。

